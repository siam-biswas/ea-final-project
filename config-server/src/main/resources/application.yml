spring:
  application:
    name: config-service

  cloud:
    config:
      server:
        git:
          uri: https://github.com/siam-biswas/ea-final-project
          default-label: main
          skip-ssl-validation: true
          search-paths: configs



server:
  port: 8888
